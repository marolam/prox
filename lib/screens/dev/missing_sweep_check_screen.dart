import "package:flutter/material.dart";
import "package:prox/widgets/placeholder_route_screen.dart";

class MissingSweepCheckScreen extends StatelessWidget {
  const MissingSweepCheckScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderRouteScreen(title: "Missing sweep", message: "Missing sweep placeholder.");
}