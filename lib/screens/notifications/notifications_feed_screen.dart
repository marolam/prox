import "package:flutter/material.dart";
import "package:prox/widgets/placeholder_route_screen.dart";

class NotificationsFeedScreen extends StatelessWidget {
  const NotificationsFeedScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderRouteScreen(title: "Notifications", message: "Notifications feed placeholder.");
}