import "package:flutter/material.dart";
import "package:prox/widgets/placeholder_route_screen.dart";

class ReleaseCandidateChecklistScreen extends StatelessWidget {
  const ReleaseCandidateChecklistScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderRouteScreen(title: "Release checklist", message: "Release checklist placeholder.");
}