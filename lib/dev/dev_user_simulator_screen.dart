import "package:flutter/material.dart";
import "package:prox/widgets/placeholder_route_screen.dart";

class DevUserSimulatorScreen extends StatelessWidget {
  const DevUserSimulatorScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderRouteScreen(title: "User simulator", message: "Simulator placeholder.");
}